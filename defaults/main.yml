---


certbot__install_from_repo: yes

# not yet implemented
#certbot__install_from_src: no

certbot__bin: "{{ '/usr/bin/certbot' if certbot__install_from_repo else '/opt/certbot-auto' }}"

# run in testing/staging env only ... will result in invalid certs
certbot__testing: yes


# map of webroot => lsit of names
# for each webroot one cert will be generated
#certbot__certs:
#  /srv/www/dom1:
#    - a.dom1.tld1
#    - b.dom1.tld1
#    - a.dom2.tld2
#  /srv/www/dom3
#    - dom3.tld3


certbot__testing_args:
  - "--staging"

certbot__args:
  - "--non-interactive"
  - "--quiet"
  - "--expand"
  - "{{ certbot__testing_args | join(' ') if certbot__testing }}"


certbot__debug: yes

# vim: tabstop=2 expandtab shiftwidth=2 softtabstop=2 smartindent nu
